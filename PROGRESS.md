# 進度手記（跨裝置同步用）

> 這份是雲端 Claude 與桌機 Claude 共用的**唯一真相來源**，任何一邊記錄/查詢進度都讀寫這份，避免跟 Obsidian 雙寫漂移。
> ⚠️ 這是公開 repo，**不放密碼、學生個資、內部行政機密**——那類內容只留在 Obsidian 或本機。

## ⏯️ 上次做到哪

### 🗓️ 2026-09-17｜KJ 法課堂便利貼牆（已正式部署上線）

**最後動作**：使用者本機測試多輪、修了大量問題與體驗優化後，確認可以上線，已合併 `claude/app-requirements-discussion-71by31` → `master` → `gh-pages`，GitHub Pages 已重新建置。

**正式網址：**
- 老師後台：`https://nicole-hsu.github.io/claude-projects/tools/kj-affinity-board/teacher.html`
- 學生端：`https://nicole-hsu.github.io/claude-projects/tools/kj-affinity-board/index.html`（實際使用時網址會帶 `?r=<討論室代碼>`，由老師端 QR/連結產生）

**本機測試階段（09-17）修過的問題與加過的功能，摘要如下（完整內容看 git log，逐條 commit message 都寫了原因）：**
- 分類欄位改成「先發散收集便利貼、內容夠了再依實際主題命名分類」，不再要求開議題時就先設好分類（這是 KJ 法精神的修正，不是單純 bug；原本一次性 batch 寫入分類欄位也曾實測失敗過，改成隨時可加之後這種失敗不再是死路）
- 便利貼之間可以先「靠在一起」成群組（不用先有分類名稱），群組可以命名成正式分類、也可以個別退出
- 老師後台從「純文字監看清單」改成跟學生一樣的視覺便利貼牆，可以投影、可以直接歸類、可以刪除不當內容
- 「議題」改成清單瀏覽（可以看過去開過的所有議題），不是每次都攤開全部內容；開新議題表單預設收起來，議題展開後可以收合
- 參與狀況：列出全班名單（不只是已加入的），標示尚未加入／已加入 0 則／N 則，可以點名字高亮該生所有便利貼（原本想用顏色區分每個人，但 45 人要 45 種顏色不實際，改用點選高亮）；名單本身預設收合避免洗版
- 班級名單改成「學號＋姓名」格式、選班級自動帶出既有名單、已有名單時預設收合
- 每人發文則數上限可以隨時調整、即時生效
- 便利貼顏色改成依便利貼 ID 算（不是排列順序），避免規律色條看起來像電腦排的
- 版面：字級調大、卡片加寬（老師端 1200px、學生端便利貼牆 1000px）、加「複製連結」按鈕方便發給學生
- 學生端用詞「房間」全部改成「討論室」

**已知還沒做的（下次有需要再做）：**
- client 端內容過濾黑名單
- 發散/收斂狀態機＋老師鎖定／還原
- 吉祥物插畫

**程式碼位置**：`tools/kj-affinity-board/`（`index.html` 學生端、`teacher.html` 老師端、`firestore.rules`、`firestore.indexes.json`、`README.md`）

**Firebase 專案**：`kj-affinity-board`（新開的，不是本檔案舊版寫的 `my-teaching-tools`——那個專案在帳號底下根本不存在，是舊版筆記的錯誤資訊）。Firestore 規則與索引已部署，Google 登入已開通，`nicole-hsu.github.io` 已在授權網域清單裡。

---

### 產品定位與規格

- B 版本——學生也能拖曳（點選）分類（保留 KJ 法「共同歸納」精神，非老師單向示範）
- 老師擁有「鎖定／還原」權限；用「發散／收斂」階段狀態機管理——**這個狀態機本身還沒做**，排在正式上線之後

**身份與資料治理：**
- 老師登入：Google 登入
- 學生加入：老師預先匯入**長期班級**名單（學號+姓名，欄位叫 `studentNo`），建一次重複用
- 學生用下拉選單選自己的學號姓名進場，即時同步
- 顯示範圍：**全班都看得到真實姓名＋學號**（不去識別化，使用者明確要求）
- 冒名輕量防護：學生選定名字後鎖住該名字（記 `deviceToken`），同一堂課不能被第二台裝置重複選走。不做正式帳號登入驗證
- 資料保留：不自動刪除，使用者之後自行手動清

**內容治理：** 便利貼 40 字上限、每人發文則數上限（老師可設定，0＝不限，用 Firestore transaction 防超發）。client 端基本髒話黑名單過濾**還沒做**。

**討論室機制：** 老師開議題產生代碼＋連結＋QR code，學生掃碼或手動輸入代碼加入。

---

### 已知限制（MVP 範圍內的取捨）

- 拖曳分類改成「點選」：手機瀏覽器對原生 HTML5 drag-and-drop 支援不穩定，改成「點便利貼→點分類欄位」
- 每人發文則數上限用 transaction + 規則做，理論上有極小機率的競爭條件被繞過，課堂內部工具風險低，先接受
- 內容黑名單過濾還沒寫，目前只能靠老師事後刪除不當便利貼
- 吉祥物插畫沒做，只有便利貼本身的卡通感（不對稱圓角、彈跳動畫、折角）

**尚待使用者自行確認（不是 Claude 能決定的）：**
- 個資法／校內規範——存真實姓名+學號且長期保留，建議跟學校既有規範或家長同意書對一下

---

## 🧪 怎麼跑起來測（給任何要接手測試的人）

```
cd tools/kj-affinity-board
python -m http.server 8000
```
開 `http://localhost:8000/teacher.html`（不要用 `file://` 直接開，Google 登入會因為網域未授權失敗）。學生端開 `http://localhost:8000/index.html?r=<討論室代碼>`。

正式環境直接用上面的 GitHub Pages 網址即可，不用架本機伺服器。

---

## ➡️ 下一步
- 使用者實際帶到課堂上用一次，確認學校網路能連到 GitHub Pages 和 Firebase（建議正式上課前先用學校 Wi-Fi 排練一次）
- 視實際使用情況決定要不要做：client 端內容過濾黑名單、發散/收斂狀態機＋鎖定、吉祥物插畫
