# 進度手記（跨裝置同步用）

> 這份是雲端 Claude 與桌機 Claude 共用的**唯一真相來源**，任何一邊記錄/查詢進度都讀寫這份，避免跟 Obsidian 雙寫漂移。
> ⚠️ 這是公開 repo，**不放密碼、學生個資、內部行政機密**——那類內容只留在 Obsidian 或本機。

## ⏯️ 上次做到哪

### 🗓️ 2026-09-17｜KJ 法課堂便利貼牆（已正式上線，白名單登入已啟用）

**現況**：完整上線使用中。老師與學生兩端功能齊全，Firebase 專案 `kj-affinity-board`（Firestore + Google 登入）已接上，`master`／`gh-pages` 同步，經過大量實測與修正。

**正式網址：**
- 老師後台：`https://nicole-hsu.github.io/claude-projects/tools/kj-affinity-board/teacher.html`
  - 短網址：`https://nicole-hsu.github.io/claude-projects/kj.html`（自動轉址，方便分享給其他老師）
- 學生端：`https://nicole-hsu.github.io/claude-projects/tools/kj-affinity-board/index.html?r=<討論室代碼>`

---

### ⚠️ 重大修正：資料模式從「真實姓名＋學號」改回「完全匿名座號」

09-17 討論中途一度定案「全班顯示真實姓名＋學號」，**後來使用者推翻，改回完全匿名，只用座號**，跟 `AI共用大腦` 的預設政策（學生資料去識別化）一致。目前系統**完全沒有姓名、學號欄位**，`students`/`joined`/`notes` 都只有 `seatNo` 一個識別欄位。CLAUDE.md 已同步移除之前記錄的「例外」說明。

---

### 身份與資料治理（最終版）

- 老師登入：Google 登入，**白名單制**——只有管理者（寫死 `nicolehsu2004@gmail.com`）新增到 `allowedEmails` 集合的信箱才能登入；管理者登入會看到「授權信箱管理」卡片可自行新增/移除。前端+Firestore 規則雙重擋。
- 班級名單：老師直接輸入座號（支援 `1-45` 範圍語法自動展開、逗號或換行分隔皆可），**儲存名單是整批覆蓋**（不在新清單裡的舊座號會被砍掉，方便清理改版前的舊資料）
- 學生選座號**不鎖裝置**：同一座號可以在手機＋平板同時使用（使用者明確要求，因為學生可能多裝置切換），下拉選單顯示所有座號、已被選過的會標註「已有人加入｜若非本人使用，請通知老師」但**仍可選**，靠人工提醒而非技術鎖死
- 老師端「參與狀況」名單每個座號旁有「**解除**」按鈕：清掉該座號的加入紀錄，讓正確的人可以重選（但不影響已發出去的便利貼內容，只是解除「目前是誰在用」的登記）
- **資料保留**：不自動刪除，使用者之後自行手動清（議題、班級都各自有刪除功能，見下）

---

### 這次新增/修正的功能與 bug（完整清單看 git log，每條都寫了原因）

**核心功能：**
- 分類欄位「先發散、後命名」：不再要求開議題時就先設好分類，便利貼可以先「靠在一起」成群組再命名成正式分類，群組可個別退出
- 老師後台便利貼牆跟學生端視覺一致（可投影），可直接歸類、刪除不當內容、**點座號可改成正確座號**（配合 transaction 同步調整新舊座號的參與則數，避免資料兜不起來）
- 議題改成清單瀏覽（不再自動展開全部內容），可收合、**可刪除**（連帶刪掉子集合）；**班級也可以刪除**（連帶刪掉底下所有議題）
- 「重新計算」按鈕：直接數便利貼牆實際內容，校正「參與狀況」跟便利貼數量對不上的情況（一鍵修正各種資料飄移，不用逐一個案處理）
- 每人發文則數上限可隨時調整、即時生效
- 便利貼牆匯出成 CSV（座號、內容、分類、發布時間）
- 議題與便利貼都加上時間戳記顯示

**介面／文字：**
- 字級調大（根字級 16→19px）、版面加寬（老師端 1200px、學生端便利貼牆 1000px），兩端都測過手機寬度不會跑版
- 「房間」全面改稱「討論室」；「開一個新房間」→「開新議題」
- 標題列加「← 換班級」快速跳回班級選擇

**踩過的坑（給下一個接手的人參考）：**
- `firebase_deploy({ only: "firestore:rules" })` 在這個環境會回報 success **但實際沒生效**，一定要用 `only: "firestore"`，部署後務必用 `firebase_get_security_rules` 讀回來比對，不要只信 deploy 的回報
- `firebase_init` 只帶 `rules` 參數、沒帶 `indexes` 會把既有索引洗成空陣列，兩個要嘛一起帶，要嘛直接改檔案再單獨部署
- 改資料庫欄位（例如 seatNo 改名/座號模式切換）時，舊資料不會自動跟著改，前端要對缺欄位的舊資料做 fallback，不然會直接當機（例如 `.localeCompare` 對 `undefined` 噴錯）
- 改一則便利貼的座號時，容易漏掉同步調整 `joined.noteCount` 這個獨立快取的計數器——凡是「改變歸屬」的操作都要記得順便調計數，不然參與統計會兜不起來

---

### 已知限制／還沒做的

- client 端內容過濾黑名單（髒話擋詞）還沒寫，目前只能靠老師事後刪除
- 發散/收斂階段狀態機＋老師鎖定／還原，還沒做
- 吉祥物插畫沒做，只有便利貼本身的卡通感

**尚待使用者自行確認：**
- 個資法／校內規範——雖然已經改回匿名座號、降低風險，但長期保留資料是否符合校內規範，建議還是跟學校確認一次

---

## 🧪 怎麼跑起來測

```
cd tools/kj-affinity-board
python -m http.server 8000
```
開 `http://localhost:8000/teacher.html`（不要用 `file://` 直接開，Google 登入會因為網域未授權失敗）。正式環境直接用短網址或完整網址即可，不用架本機伺服器。

---

## ➡️ 下一步
- 使用者實際帶到課堂用一次，確認學校網路能連到 GitHub Pages 和 Firebase（建議正式上課前先用學校 Wi-Fi 排練一次）
- 視實際使用情況決定要不要做：內容黑名單、發散/收斂狀態機、吉祥物插畫
