# 進度手記（跨裝置同步用）

> 這份是雲端 Claude 與桌機 Claude 共用的**唯一真相來源**，任何一邊記錄/查詢進度都讀寫這份，避免跟 Obsidian 雙寫漂移。
> ⚠️ 這是公開 repo，**不放密碼、學生個資、內部行政機密**——那類內容只留在 Obsidian 或本機。

## ⏯️ 上次做到哪

### 🗓️ 2026-09-17｜KJ法課堂討論工具（MVP 規格定案，準備進入開發）

**最後動作**：完成所有待答問題討論，MVP 完整規格與 Firestore 資料結構已定案，尚未開工寫程式

**產品定位（延續 09-16）：**
- B 版本——學生也能拖曳分類（保留 KJ 法「共同歸納」精神，非老師單向示範）
- 老師擁有「鎖定／還原」權限；用「發散／收斂」階段狀態機管理，非逐張便利貼設權限
- 技術路徑：沿用現有 Firebase（`my-teaching-tools`）+ Firestore 即時監聽

**本次新定案（09-17）：**

1. **身份與資料治理**
   - 老師登入：Google 登入
   - 學生加入：老師預先匯入**長期班級**名單（姓名+學號），建一次重複用，不用每堂課重傳
   - 學生用下拉選單選自己的姓名學號進場（不手打，避免打錯字/亂輸入）
   - 顯示範圍：**全班都看得到真實姓名＋學號**（不去識別化——推翻 09-16 原本「座號＋班級代號」的建議）
     - ⚠️ 已知取捨：公開真實身份可能讓學生發言比較保守，之後若發現問題可加「匿名模式」開關
   - 冒名風險的輕量防護：學生選定名字後鎖住該名字（記 deviceToken），同一堂課不能被第二台裝置重複選走。**不做正式帳號登入驗證**——課堂即時進行、老師現場可監督，接受這個風險等級
   - 資料保留：**不自動刪除**，長期留著（也順便當點名/參與度紀錄），使用者之後自行手動清除

2. **點名／出席**
   - 學生一選定名字（進房間）就算「已到」，跟他有沒有發便利貼分開計算
   - 老師端要有獨立的「已加入名單」清單，跟便利貼牆分開顯示

3. **內容治理**
   - 便利貼字數上限：**40 字**
   - 基本內容過濾：client 端擋髒話/敏感詞黑名單，不做 AI 語意審核（課堂內部工具，風險等級不需要那麼高）
   - 每人發文則數上限：**老師可設定**（0 或不設 = 不限制），跟身份治理算同一組資料結構

4. **房間／教室機制（新功能，原本 MVP 沒有）**
   - 老師開課時系統產生**房間代碼＋連結**，學生要有代碼/連結才能進去，避免跟別班或外人混在一起
   - 老師打題目後，畫面顯示題目 ＋ 自動產生**連結和 QR code**，學生手機/平板掃碼直接進房間
   - 這是「房間代碼」需求的呈現方式，不是額外大功能，已計入 MVP

5. **視覺風格**
   - 便利貼採卡通/手繪感：四角不對稱微翹、拖曳放下有輕微彈跳動畫、右上角小折角、顏色隨機繽紛（純視覺區分，不代表分類意義）
   - 更完整的吉祥物插畫角色列為非 MVP 必要，之後有空再做

**Firestore 資料結構草案：**
```
classes/{classId}
  name, teacherId, createdAt
  students/{studentId}          # 姓名+學號花名冊，老師匯入
    name, seatNo

rooms/{roomId}                  # 每次開課一間房，對應一個問題
  classId, code                 # 短碼，組連結/QR用
  question
  maxNotesPerStudent
  active, createdAt

  joined/{studentId}            # 出席名單
    name, seatNo, deviceToken, joinedAt

  notes/{noteId}                # 便利貼
    studentId, name, seatNo, text(≤40字)
    categoryId(null=未分類), color, createdAt

  categories/{categoryId}       # 分類欄位
    label, order
```

**安全規則邏輯（Firestore Security Rules 要實作的檢查）：**
- 寫入 notes：studentId 必須在該 room 的 joined 裡、deviceToken 要對得上、該學生現有便利貼數 < maxNotesPerStudent
- 寫入 joined（選名字）：若該 studentId 已被其他 deviceToken 佔用且未逾時，拒絕

**開發階段順序**：MVP（上述全部）→ 發散/收斂狀態機＋鎖定 → 還原機制 → 手動關聯線 → （選做）語意分群輔助 → （選做）多課程/多老師管理 → （選做）更完整吉祥物插畫

**尚待使用者自行確認（不是 Claude 能決定的）：**
- 個資法／校內規範——存真實姓名+學號且長期保留，建議跟學校既有規範或家長同意書對一下

---

## ➡️ 下一步
- 開始實作：Firebase 專案（`my-teaching-tools`）建立 classes/rooms/notes 對應的 Firestore collection 與 Security Rules
- 前端：老師端（Google 登入、匯入名單、開房間、發題目+QR）與學生端（掃碼進房、選名字、發便利貼、拖曳分類）兩個介面
