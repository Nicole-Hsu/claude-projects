# 進度手記（跨裝置同步用）

> 這份是雲端 Claude 與桌機 Claude 共用的**唯一真相來源**，任何一邊記錄/查詢進度都讀寫這份，避免跟 Obsidian 雙寫漂移。
> ⚠️ 這是公開 repo，**不放密碼、學生個資、內部行政機密**——那類內容只留在 Obsidian 或本機。

## ⏯️ 上次做到哪

### 🗓️ 2026-09-17｜KJ 法課堂便利貼牆（程式碼已寫完、Firebase 已接上，手動測試中，還沒部署上線）

**最後動作**：老師端／學生端完整流程可以在本機（`localhost:8000`，見下方「怎麼跑起來測」）跑通，測試過程中修了好幾個問題，其中一個是流程設計上的錯誤（見下方「重要修正」）。**還沒合併到 `master` / 部署到 GitHub Pages 正式網址**，使用者說要先自己測完再決定。

**程式碼位置**：`tools/kj-affinity-board/`（`index.html` 學生端、`teacher.html` 老師端、`firestore.rules`、`firestore.indexes.json`、`README.md`）

**Firebase 專案**：`kj-affinity-board`（新開的，不是本檔案舊版寫的 `my-teaching-tools`——那個專案在帳號底下根本不存在，是舊版筆記的錯誤資訊）。Firestore 規則與索引已部署，Google 登入已開通。

---

### 產品定位與規格（09-16 討論、09-17 定案，之後又修正一處）

- B 版本——學生也能拖曳分類（保留 KJ 法「共同歸納」精神，非老師單向示範）
- 老師擁有「鎖定／還原」權限；用「發散／收斂」階段狀態機管理——**這個狀態機本身還沒做**，目前是 MVP 之後的項目

**身份與資料治理：**
- 老師登入：Google 登入 ✅ 已實作
- 學生加入：老師預先匯入**長期班級**名單（姓名+學號，欄位叫 `studentNo` 不是 `seatNo`），建一次重複用
- 學生用下拉選單選自己的姓名學號進場，**即時同步**（老師事後補名單，學生畫面不用重新整理就會看到）
- 顯示範圍：**全班都看得到真實姓名＋學號**（不去識別化，這是使用者明確要求，推翻更早之前「座號＋班級代號」的建議）
- 冒名輕量防護：學生選定名字後鎖住該名字（記 `deviceToken`），同一堂課不能被第二台裝置重複選走。不做正式帳號登入驗證
- 資料保留：不自動刪除，使用者之後自行手動清

**點名／出席：** 老師後台「參與狀況」卡片會列出全班名單（不只是已加入的），標示「尚未加入」／「已加入，0 則」／「N 則」，0 則的用紅字排最前面，一眼看出誰沒參與。這是使用者中途追加的需求，已實作。

**內容治理：** 便利貼 40 字上限、每人發文則數上限（老師可設定，0＝不限，用 Firestore transaction 防超發）。client 端基本髒話黑名單過濾**還沒做**。

**房間機制：** 老師開房間產生代碼＋連結＋QR code，學生掃碼或手動輸入代碼加入。

---

### ⚠️ 重要修正：分類欄位不能在開房間時就要求先設好

使用者指出這違反 KJ 法本身的精神——親和圖是**先發散收集便利貼，再從實際內容歸納出分類**，不是先射飛鏢決定分類名稱再把便利貼塞進去。

已修正：開房間表單拿掉了「分類欄位」輸入，房間一開始就是 0 個分類（這是**正常狀態**，不是錯誤/需要提示紅字的狀況）。老師端「目前房間」卡片新增了隨時可以新增/刪除分類的 UI（chip 形式），等便利貼收集得差不多、看到實際主題浮現後再依內容新增分類。學生端在還沒有分類欄位時顯示「老師還在收集大家的想法」的友善提示。

**這個修正同時解決了另一個實測 bug**：原本開房間時分類欄位是用一次性 `batch.commit()` 寫入，實測發現某個房間（SBNMXD）的分類欄位莫名其妙沒寫進去、原因沒查出來，導致學生端看不到任何分類欄位可以歸類。既然分類欄位改成「隨時可加」的機制，這種失敗也不再是死路。

---

### 測試中發現並修掉的其他問題（給下一個接手的 Claude 參考，別重踩）

- 開房間前沒檢查班級名單是不是空的，導致生出一個學生完全加入不了的房間——已加防呆
- 老師重新整理頁面、重選班級，原本開的房間會憑空消失（純前端記憶體狀態，沒去 Firestore 讀回來）——已修成選班級時自動查該班最近一次 `active` 房間並還原畫面（這個查詢需要一個 Firestore 複合索引 `rooms: classId+active+createdAt`，已加進 `firestore.indexes.json` 並部署；索引建立有幾分鐘的延遲，這段期間查詢會失敗，已改成安靜略過不擋畫面）
- QR code 測試時注意：`localhost` 只有出題那台電腦連得到，手機在同一區網也不一定連得到（尤其學校網路常把有線/無線隔開）——真正要用手機測 QR，必須部署到 GitHub Pages 正式網址（`nicole-hsu.github.io`，已在 Firebase Auth 授權網域清單裡，可以直接部署）

---

### 已知限制（MVP 範圍內的取捨）

- 拖曳分類改成「點選」：手機瀏覽器對原生 HTML5 drag-and-drop 支援不穩定，改成「點便利貼→點分類欄位」
- 每人發文則數上限用 transaction + 規則做，理論上有極小機率的競爭條件被繞過，課堂內部工具風險低，先接受
- 老師只能刪除便利貼做基本管理，內容黑名單過濾還沒寫
- 吉祥物插畫沒做，只有便利貼本身的卡通感（不對稱圓角、彈跳動畫、折角）

**尚待使用者自行確認（不是 Claude 能決定的）：**
- 個資法／校內規範——存真實姓名+學號且長期保留，建議跟學校既有規範或家長同意書對一下

---

## 🧪 怎麼跑起來測（給任何要接手測試的人）

```
cd tools/kj-affinity-board
python -m http.server 8000
```
開 `http://localhost:8000/teacher.html`（不要用 `file://` 直接開，Google 登入會因為網域未授權失敗）。學生端開 `http://localhost:8000/index.html?r=<房間代碼>`。

---

## ➡️ 下一步
- 使用者自己測完整輪流程（老師開房間、發問、產生分類、學生發便利貼、歸類、參與統計）
- 測試通過後：合併 `claude/app-requirements-discussion-71by31` 到 `master`，同步到 `gh-pages`，正式上線到 `nicole-hsu.github.io/claude-projects/tools/kj-affinity-board/`，才能真正用手機在任何網路測 QR code
- 之後：client 端內容過濾黑名單、發散/收斂狀態機＋鎖定
